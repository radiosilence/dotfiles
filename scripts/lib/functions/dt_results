# Tally line for fanned-out work. The caller counts failures because only it
# knows what failure means.
autoload -Uz dt_ok dt_warn
local ok=$1 failed=$2 verb=$3
if (( failed )); then
  dt_warn "$verb $ok files ($failed failed)"
else
  dt_ok "$verb $ok files"
fi
