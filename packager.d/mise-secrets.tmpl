# Resolved by `task secrets:populate` via `op inject` (single Touch ID for all keys).
# Add lines here to have them populated into ~/mise.local.toml on next converge.
BUF_TOKEN = "{{ op://Personal/buf.build/token }}"
NODE_AUTH_TOKEN = "{{ op://Personal/npm.pkg.github.com/password }}"
NPM_AUTH_TOKEN = "{{ op://Personal/npm/token }}"
