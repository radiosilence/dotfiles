# KEY = "{{ op://Vault/item/field }}" lines here get op-injected into
# ~/.config/mise/conf.d/secrets.toml (every shell's env) by `task secrets:populate`.
# Prefer per-command `op run` injection (see buf in 20-op.zsh) for anything else.
