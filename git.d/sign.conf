[gpg "ssh"]
program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign

[gpg]
format = ssh

[commit]
gpgsign = true

# tag.gpgsign and user.signingkey are set in local git config
# (machine-specific, managed via 1Password SSH agent)
