#!/bin/sh
# Bootstrap script - run this on a fresh system
# Usage: ./setup

set -e

DOTFILES_DIR="$(cd "$(dirname "$0")" && pwd)"

echo "🚀 Dotfiles Bootstrap"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# Check if we have cargo/rust already
if command -v cargo >/dev/null 2>&1; then
  echo "✓ Rust/Cargo found, building and running upd..."
  cd "$DOTFILES_DIR/tooling-rust"
  cargo install --path . --root .. --bin upd --force
  cd "$DOTFILES_DIR"
  exec "$DOTFILES_DIR/bin/upd" "$@"
fi

# Bootstrap mode - install Rust first
echo "→ Rust not found, installing rustup..."
echo ""

curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable

# Source cargo env
. "$HOME/.cargo/env"

echo ""
echo "✓ Rust installed"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "→ Building upd..."
echo ""

cd "$DOTFILES_DIR/tooling-rust"
cargo install --path . --root .. --bin upd --force

echo ""
echo "✓ upd built"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "→ Running bootstrap..."
echo ""

cd "$DOTFILES_DIR"
exec "$DOTFILES_DIR/bin/upd" "$@"
