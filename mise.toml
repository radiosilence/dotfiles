[tools]

[tasks.link]
description = "Symlink dotfiles and configs to their destinations"
run = '''
#!/usr/bin/env bash
set -euo pipefail

DOTFILES="{{config_root}}"

# --- Dotfiles (.* → ~) ---
for f in "$DOTFILES"/.*; do
  name=$(basename "$f")
  case "$name" in
    .|..|.git|.gitignore|.github|.vscode|.sonarlint|\
    .editorconfig|.codeowners-lsp|.ruby-lsp|.crates.toml|.crates2.json)
      continue ;;
  esac

  target="$HOME/$name"

  if [ -L "$target" ] && [ "$(readlink "$target")" = "$f" ]; then
    continue
  fi

  rm -rf "$target"
  ln -s "$f" "$target"
  printf "  \033[32m󰌷\033[0m %s\n" "$name"
done

# --- Config dirs (config.d/* → ~/.config/*) ---
mkdir -p "$HOME/.config"

if [ -d "$DOTFILES/config.d" ]; then
  for f in "$DOTFILES/config.d"/*; do
    [ -e "$f" ] || continue
    name=$(basename "$f")
    [ "$name" = "launchd" ] && continue
    target="$HOME/.config/$name"

    if [ -L "$target" ] && [ "$(readlink "$target")" = "$f" ]; then
      continue
    fi

    rm -rf "$target"
    ln -s "$f" "$target"
    printf "  \033[32m󰌷\033[0m ~/.config/%s\n" "$name"
  done
fi

# --- Gitconfig include ---
[ -f "$HOME/.gitconfig" ] || touch "$HOME/.gitconfig"
if ! grep -q '.dotfiles' "$HOME/.gitconfig" 2>/dev/null; then
  printf '\n[include]\npath = ~/.dotfiles/git.d/core.conf\n' >> "$HOME/.gitconfig"
  printf "  \033[32m󰄬\033[0m gitconfig include\n"
fi

# --- SSH config include ---
mkdir -p "$HOME/.ssh"
if [ ! -f "$HOME/.ssh/config" ]; then
  touch "$HOME/.ssh/config"
  chmod 600 "$HOME/.ssh/config"
fi
if ! grep -q '.dotfiles' "$HOME/.ssh/config" 2>/dev/null; then
  printf '\nInclude ~/.dotfiles/ssh.d/*.conf\n' >> "$HOME/.ssh/config"
  printf "  \033[32m󰄬\033[0m ssh config include\n"
fi

# --- Brewfile (macOS) ---
if [ "$(uname)" = "Darwin" ] && [ -f "$DOTFILES/Brewfile" ]; then
  target="$HOME/Brewfile"
  if ! [ -L "$target" ] || [ "$(readlink "$target")" != "$DOTFILES/Brewfile" ]; then
    ln -sf "$DOTFILES/Brewfile" "$target"
    printf "  \033[32m󰄬\033[0m Brewfile\n"
  fi
fi

# --- Launchd agents (macOS) ---
if [ "$(uname)" = "Darwin" ] && [ -d "$DOTFILES/config.d/launchd" ]; then
  agents="$HOME/Library/LaunchAgents"
  mkdir -p "$agents"
  for plist in "$DOTFILES/config.d/launchd"/*.plist; do
    [ -f "$plist" ] || continue
    name=$(basename "$plist")
    dest="$agents/$name"
    if [ -f "$dest" ] && cmp -s "$plist" "$dest"; then
      continue
    fi
    cp "$plist" "$dest"
    launchctl unload "$dest" 2>/dev/null || true
    launchctl load "$dest" 2>/dev/null || true
    printf "  \033[32m󰄬\033[0m launchd: %s\n" "$name"
  done
fi

# --- Claude Code hooks (inject into ~/.claude/settings.json) ---
claude_settings="$HOME/.claude/settings.json"
if command -v jq >/dev/null 2>&1 && [ -f "$claude_settings" ]; then
  hooks_changed=false

  # gastown: notify Mayor on file changes (no-op outside gastown rigs)
  if ! jq -e '.hooks.PostToolUse[]?.hooks[]? | select(.command | contains("gastown-file-changed"))' "$claude_settings" >/dev/null 2>&1; then
    jq '
      .hooks //= {} |
      .hooks.PostToolUse //= [] |
      .hooks.PostToolUse += [{"matcher": "Write|Edit", "hooks": [{"type": "command", "command": "~/.dotfiles/hooks/gastown-file-changed.sh"}]}]
    ' "$claude_settings" > "$claude_settings.tmp" && mv "$claude_settings.tmp" "$claude_settings"
    hooks_changed=true
    printf "  \033[32m󰄬\033[0m claude hooks: gastown\n"
  fi
fi

# --- Sheldon plugins ---
if command -v sheldon >/dev/null 2>&1; then
  sheldon source >/dev/null 2>&1 && printf "  \033[32m󰄬\033[0m sheldon\n" || true
fi

printf "  \033[32m󰄬\033[0m link complete\n"
'''


[tasks.reinstall-bins]
description = "Build and install rust binaries from crates/"
run = '''
#!/usr/bin/env bash
set -euo pipefail
if ! command -v cargo >/dev/null 2>&1; then
    printf "  \033[31m󰅖\033[0m cargo not found\n"
    exit 1
fi
printf "  \033[35m󱁤\033[0m building rust binaries\n"
cargo install --path "{{config_root}}/crates" --bins --root "{{config_root}}"
printf "  \033[32m󰄬\033[0m binaries installed\n"
'''

[tasks.use-ssh]
description = "Switch dotfiles remote from HTTPS to SSH"
run = '''
#!/usr/bin/env bash
set -euo pipefail
cd "{{config_root}}"
current=$(git remote get-url origin)
if echo "$current" | grep -q "^git@"; then
  printf "  \033[32m󰄬\033[0m already using SSH\n"
  exit 0
fi
ssh_url=$(echo "$current" | sed -E 's|https://github.com/(.+)|git@github.com:\1|')
git remote set-url origin "$ssh_url"
printf "  \033[32m󰄬\033[0m remote switched to %s\n" "$ssh_url"
'''

# =============================================================================
# System update pipeline (replaces the old ratatui TUI binary)
#
# DAG:
#   link, auth, fonts, mise, claude, tmux-plugins ── run in parallel
#   brew-bundle ──→ brew                           ── brew serialized
#   [brew, brew-bundle, mise] ──→ zsh-completions  ── completions last
#   everything ──→ upd                             ── top-level entry
# =============================================================================

[tasks."upd:link"]
description = "Symlink dotfiles"
depends = ["link"]
run = "true"

[tasks."upd:auth"]
description = "Check gh/op authentication (macOS)"
run = '''
#!/usr/bin/env bash
set -euo pipefail
[[ "$(uname)" == "Darwin" ]] || { echo "skipped (not macOS)"; exit 0; }

if command -v gh >/dev/null 2>&1; then
  if gh auth status >/dev/null 2>&1; then
    printf "  \033[32m󰄬\033[0m gh: authenticated\n"
  else
    printf "  \033[33m\033[0m gh: not authenticated (run gh auth login)\n"
  fi
fi

if command -v op >/dev/null 2>&1; then
  if op account list >/dev/null 2>&1; then
    printf "  \033[32m󰄬\033[0m op: integrated\n"
  else
    printf "  \033[33m\033[0m op: not integrated (check 1Password CLI settings)\n"
  fi
fi
'''

[tasks."upd:fonts"]
description = "Install missing fonts (macOS)"
run = '''
#!/usr/bin/env bash
set -euo pipefail
[[ "$(uname)" == "Darwin" ]] || { echo "skipped (not macOS)"; exit 0; }

FONTS_DIR="$HOME/Library/Fonts"
mkdir -p "$FONTS_DIR"

install_font() {
  local name="$1" url="$2" marker="$3"
  if [ -f "$FONTS_DIR/$marker" ]; then
    printf "  \033[32m󰄬\033[0m %s (installed)\n" "$name"
    return 0
  fi
  printf "  \033[35m󱁤\033[0m %s (downloading)\n" "$name"
  local tmpdir
  tmpdir=$(mktemp -d)
  trap "rm -rf '$tmpdir'" RETURN
  curl -fsSL "$url" -o "$tmpdir/font.zip"
  unzip -qo "$tmpdir/font.zip" -d "$tmpdir/extracted"
  find "$tmpdir/extracted" \( -iname '*.otf' -o -iname '*.ttf' \) -exec cp {} "$FONTS_DIR/" \;
  printf "  \033[32m󰄬\033[0m %s (installed)\n" "$name"
}

# Fonts from dotfiles.toml — hardcoded here to avoid needing a TOML parser in bash.
# Keep in sync with [[fonts]] entries in dotfiles.toml.
install_font "Hack Ligatured" \
  "https://github.com/gaplo917/Ligatured-Hack/releases/download/v3.003%2BNv2.1.0%2BFC%2BJBMv2.242/HackLigatured-v3.003%2BFC3.1%2BJBMv2.242.zip" \
  "HackNerdFontJBMLigatured-Regular.ttf"

install_font "Geist" \
  "https://github.com/vercel/geist-font/releases/download/geist%401.7.0/geist-font-v1.7.0.zip" \
  "Geist-Regular.otf"

install_font "Geist Mono Nerd Font" \
  "https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/GeistMono.zip" \
  "GeistMonoNerdFont-Regular.otf"
'''

[tasks."upd:brew-bundle"]
description = "Install Brewfile packages"
run = '''
#!/usr/bin/env bash
set -euo pipefail
command -v brew >/dev/null 2>&1 || { echo "skipped (no brew)"; exit 0; }
brew bundle --quiet
'''

[tasks."upd:brew"]
description = "Update/upgrade/cleanup brew"
depends = ["upd:brew-bundle"]
run = '''
#!/usr/bin/env bash
set -euo pipefail
command -v brew >/dev/null 2>&1 || { echo "skipped (no brew)"; exit 0; }
brew update --quiet
brew upgrade --greedy --quiet
brew cleanup --quiet
'''

[tasks."upd:apt"]
description = "Update apt packages"
run = '''
#!/usr/bin/env bash
set -euo pipefail
command -v apt-get >/dev/null 2>&1 || { echo "skipped (no apt)"; exit 0; }
sudo apt-get update
sudo apt-get upgrade -y
sudo apt-get autoremove -y
'''

[tasks."upd:dnf"]
description = "Update dnf packages"
run = '''
#!/usr/bin/env bash
set -euo pipefail
command -v dnf >/dev/null 2>&1 || { echo "skipped (no dnf)"; exit 0; }
sudo dnf update -y
'''

[tasks."upd:mise"]
description = "Update mise tools"
run = '''
#!/usr/bin/env bash
set -euo pipefail
command -v mise >/dev/null 2>&1 || { echo "skipped (no mise)"; exit 0; }
mise up
mise reshim
'''

[tasks."upd:claude"]
description = "Update Claude Code"
run = '''
#!/usr/bin/env bash
set -euo pipefail
command -v claude >/dev/null 2>&1 || { echo "skipped (no claude)"; exit 0; }
claude --update
'''

[tasks."upd:tmux-plugins"]
description = "Install/update tmux plugins"
run = '''
#!/usr/bin/env bash
set -euo pipefail

PLUGINS_DIR="$HOME/.tmux/plugins"
mkdir -p "$PLUGINS_DIR"

declare -A plugins=(
  [tmux-resurrect]="https://github.com/tmux-plugins/tmux-resurrect.git"
  [tmux-fzf-url]="https://github.com/wfxr/tmux-fzf-url.git"
)

for name in "${!plugins[@]}"; do
  url="${plugins[$name]}"
  dest="$PLUGINS_DIR/$name"
  if [ -d "$dest" ]; then
    git -C "$dest" pull --quiet
    printf "  \033[32m󰄬\033[0m %s (updated)\n" "$name"
  else
    git clone --quiet "$url" "$dest"
    printf "  \033[32m󰄬\033[0m %s (installed)\n" "$name"
  fi
done
'''

[tasks."upd:zsh-completions"]
description = "Regenerate zsh completions"
depends = ["upd:brew", "upd:brew-bundle", "upd:mise"]
run = '''
#!/usr/bin/env bash
set -euo pipefail
command -v regen-zsh-completions >/dev/null 2>&1 || { echo "regen-zsh-completions not found, skipping"; exit 0; }
regen-zsh-completions
'''

[tasks.upd]
description = "Update the system"
depends = [
  "upd:link",
  "upd:auth",
  "upd:fonts",
  "upd:brew",
  "upd:apt",
  "upd:dnf",
  "upd:mise",
  "upd:claude",
  "upd:tmux-plugins",
  "upd:zsh-completions",
]
run = '''
#!/usr/bin/env bash
printf "\n  \033[32m󰄬\033[0m \033[1msystem update complete\033[0m\n\n"
'''
