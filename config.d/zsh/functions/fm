local branch
branch=$(git for-each-ref --sort=-committerdate refs/heads/ \
  --format='%(committerdate:relative)|%(refname:short)' |
  column -t -s'|' |
  fzf --no-sort --prompt="merge ← " |
  awk '{ print $NF }') || return 0
[[ -n "$branch" ]] && git merge "$branch"
