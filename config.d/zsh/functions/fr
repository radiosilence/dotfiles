local branch
branch=$(git for-each-ref --sort=-committerdate refs/heads/ refs/remotes/ \
  --format='%(committerdate:relative)|%(refname:short)' |
  column -t -s'|' |
  fzf --no-sort --prompt="rebase → " |
  awk '{ print $NF }') || return 0
[[ -n "$branch" ]] && git rebase "$branch"
