[env]
# mise does not tilde-expand env values — a literal "~/..." makes claude resolve
# the path relative to cwd and silently re-onboard into ./~/
CLAUDE_CONFIG_DIR = "{{env.HOME}}/.claude-personal"
